#!/bin/bash

PARAMS="$@"
CMD="cd /src && USER=discourse RAILS_ENV=${RAILS_ENV:=development} bundle exec sidekiq -q critical -q low -q default -q high_cpu -q high_cpu_low"
docker exec -it -u discourse:discourse discourse_dev /bin/bash -c "$CMD"
